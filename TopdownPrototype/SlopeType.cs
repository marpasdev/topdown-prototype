using System;

namespace TopdownPrototype
{
    internal enum SlopeType : byte
    {
        // later possibly different types of rocks, even water for waterfalls
        None = 0,
        Stone = 1,
        Dirt = 2
    }
}
